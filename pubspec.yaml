name: ngovolunteer_app

desciption: A new Flutter project

version: 1.0.0+1

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  riverpod: ^1.0.0
  go_router: ^5.0.0
  dio: ^5.0.0
  hive: ^2.0.0
  hive_flutter: ^1.0.0
  material: ^5.0.0

dev_dependencies:
  build_runner: ^2.1.0
  json_serializable: ^6.0.0
  hive_generator: ^1.0.0
